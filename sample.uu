func test () {
    foo();
    wer = "foo";
    if(wer == 1) {
        print("wer");
    }
    while(wer == 1) {
        wer = wer + 1;
    }
    for(i; i < 4321; i = i + 1) {
        print(i);
    }
}
